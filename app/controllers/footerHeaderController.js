//controler for Login
	eshopApp.controller('FooterHeaderController',['$scope', '$routeParams','customersService',function($scope,$routeParams,customersService) {
		
		$scope.footerShow = 'true';
		$scope.footer = function(){
			return 0;
		}
}]);